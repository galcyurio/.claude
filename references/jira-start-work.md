# Jira 착수 상태 전이

Jira 키가 붙은 **구현 작업**에 착수할 때, 그 이슈가 아직 "해야 할 일"이면 **진행 중**으로 전이한다. 착수 사실을 매번 손으로 옮기지 않기 위한 규칙이다.

## 발동 조건

`~/.claude/rules/intent-gate.md`의 의도가 **명시적 구현** 또는 **버그 수정**이고, 대상 Jira 키가 확정된 경우에만 발동한다.

- 발동한다: "HDA-22577 작업해줘", `feature/HDA-22577-...` 브랜치를 만들고 구현에 들어갈 때, 방금 만든 이슈에 곧바로 착수할 때.
- 발동하지 않는다: 이해/설명·조사/확인·평가/판단처럼 이슈를 읽기만 하는 요청. 리뷰·커밋·PR 생성 같은 후속 단계.

## 내가 옮기는 상태는 "진행 중" 하나뿐이다

**`진행 중` 다음 상태는 손대지 않는다.** PR을 만들면 `검토 중`으로 넘어가는 것은 Jira Automation이 처리하므로, PR 생성 후에 상태를 옮기지도 말고 옮길지 물어보지도 않는다. 머지·배포 이후 상태도 마찬가지다.

## 절차

1. 이슈 키를 확정한다. 사용자 발화에 없으면 현재 브랜치 이름(`feature/HDA-\d+-...`)에서 뽑는다. 어느 쪽에서도 못 찾으면 조용히 skip한다 — 키를 추측하지 않는다.
2. `mcp__claude_ai_Atlassian__getJiraIssue`로 현재 상태를 확인한다. `fields.status.statusCategory.key`가 `new`("해야 할 일")가 아니면 skip한다. 이미 진행 중이거나 검토 중인 이슈를 되돌리지 않는다.
3. 담당자(`fields.assignee`)가 본인도 미지정도 아니면 전이하지 않고, 사실만 한 줄 알린 뒤 작업을 계속한다. 남의 이슈 상태를 대신 옮기지 않는다.
4. `mcp__claude_ai_Atlassian__getTransitionsForJiraIssue`로 전이 목록을 받아 `to.name`이 `진행 중`인 항목을 고른다. transition id는 워크플로우마다 다르므로 **매번 조회해서 고르고, 값을 외워 쓰지 않는다.**
5. `mcp__claude_ai_Atlassian__transitionJiraIssue`로 전이한다.
6. 결과를 응답에 한 줄 남긴다 (`HDA-22577 Backlog → 진행 중`). 전이해 놓고 말없이 넘어가지 않는다.

전이에 실패해도 작업은 멈추지 않는다. 실패 사실만 한 줄 남기고 구현을 이어간다.

## 실측 값 (2026-08-20, prndcompany)

| 항목 | 값 |
|---|---|
| cloudId | `4e8e1a3d-2b6f-40df-820b-43c476f41656` |
| "해야 할 일" 카테고리 | `statusCategory.key = "new"` — `Backlog`(id 10003), `다시 열림`(id 4) |
| 목표 상태 | `진행 중` (status id `3`) |
| HDA 워크플로우의 전이 | `작업 시작` (transition id `201`) |

transition id는 프로젝트 워크플로우마다 다르다. 위 표는 참고이고, 실제로는 4단계의 조회 결과를 쓴다.
